package test;

import global.rule;

event(GA).any(10) {
	event(B),
	event(GC).any(3) {
		event(D), 
		event(E)
	}, 
	event(GF).any(1) {
		event(H), 
		event(I)
	}
} 
event(GH).any(1){
	event(GF), 
	event(H)
}